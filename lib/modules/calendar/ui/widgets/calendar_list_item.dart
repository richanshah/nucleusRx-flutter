import '../../../../utils/exports.dart';

class CalendarListItem extends StatelessWidget {
  const CalendarListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 50.h,

    );
  }
}
